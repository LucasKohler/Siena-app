# Dados de exemplo (DEV)

Os arquivos `events.json` e `videos.json` nesta pasta são **dados de exemplo para desenvolvimento local**. Não representam calendário oficial, resultados reais nem estatísticas de visualização da associação.

Substituir por fonte oficial (CMS, integração ou banco) quando aprovado em ADR e spec de produção.
